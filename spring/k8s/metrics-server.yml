apiVersion: v1
kind: Service
metadata:
  name: spring-metrics
  labels:
    app: spring
spec:
  type: ClusterIP
  selector:
    app: spring
  ports:
    - name: http
      port: 8177
      targetPort: 8177
      protocol: TCP
    