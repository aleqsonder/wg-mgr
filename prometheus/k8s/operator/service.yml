apiVersion: v1
kind: Service
metadata:
  name: prometheus-operator
  namespace: monitoring
spec:
  selector:
    app.kubernetes.io/name: prometheus-operator
  ports:
    - name: http
      targetPort: http # of deployment
      port: 8080
  clusterIP: None # headless to scrape directly
